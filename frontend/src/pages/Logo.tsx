import React from "react";

/**
 * Logo FriEnds — reprend exactement le style de l'icône d'accueil :
 * carré arrondi en dégradé violet → orange, avec "Fr" en blanc.
 */
export default function Logo() {
  return (
    <div className="auth-logo">
      <span>Fr</span>
    </div>
  );
}
